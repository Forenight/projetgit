{% extends '::base.html.twig' %}

{% block body -%}
{{parent()}}
    <h1>{{ entity.promotion }} - {{ entity.groupe }}</h1>
    <div class="line"></div>
<br>
                <h3>Description</h3>
                <p>{{ entity.description }}</p>

        <ul class="record_actions">
    <li>
        <a style="color:black" class="btn btn-default" href="{{ path('agenda') }}">
            Retour à la liste
        </a>
    </li>
    <li>
        <a style="color:black" class="btn btn-default" href="{{ path('agenda_edit', { 'id': entity.id }) }}">
            Modifier
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
