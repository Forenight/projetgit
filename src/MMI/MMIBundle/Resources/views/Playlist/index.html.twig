{% extends '::base.html.twig' %}

{% block body -%}
{{ parent () }}
    <h1>Playlist list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Description</th>
                <th>Playlist</th>
                <th>Auteur</th>
                <th>Durée</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('playlist_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.Playlist }}</td>
                <td>{{ entity.auteur }}</td>
                <td>{{ entity.duree }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('playlist_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('playlist_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('playlist_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
