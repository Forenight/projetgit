{% extends '::base.html.twig' %}

{% block body -%}
{{parent()}}
    <h1>{{ entity.nom }}</h1>
    <div class="line"></div>
<br>

                <h3>Description</h3>
                <p>{{ entity.description }}</p>
                <h3>Date</h3>
                <p>{{ entity.date|date('Y-m-d H:i:s') }}</p>
                <h3>Categorie</h3>
                <p>{{ entity.categorie }}</p>

        <ul class="record_actions">
    <li>
        <a style="color:black" class="btn btn-default" href="{{ path('news') }}">
            Retour à la liste
        </a>
    </li>
    <li>
        <a style="color:black" class="btn btn-default" href="{{ path('news_edit', { 'id': entity.id }) }}">
            Modifier
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
